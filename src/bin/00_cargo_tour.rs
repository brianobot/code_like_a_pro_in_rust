// you can use the warn(mission_docs) attribute to force the compiler to raise warnings when a public item is missing docs

fn main() {
    // use a different channel on your project like so
    // cargo +<channel> <command>
    // 
    println!("There are {} CPUs", num_cpus::get());
    
    // you can use cargo build --target <target> to compile for a particular target
}