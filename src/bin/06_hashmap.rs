use std::collections::HashMap;



fn main() {
    let _map = HashMap::<String, String>::default();
    // when creating a hashmap, you can pass in your own Hashing function through the hashstate attribute
    // 
}